# Issues #17–#29 and PR #27 — triage and order — 2026-09-10

Two users installed 4.0.0 fresh on 2026-09-09 and filed thirteen issues and
one pull request. Everything here is fixed on `work-rounds` in the order
below, each with its test and, where the design changes, its dated note.

| # | what | severity | plan |
|---|---|---|---|
| 17 | fresh install: every capture refused (`legacy_protocol_unquiesced`) until V3 adoption is run by hand | critical, core loop | installer adopts a fresh vault; doctor names the condition; README/CHANGELOG corrected |
| 21 | nightly waits 5 min for compile, then counts a healthy slow compile as failures | high | wait follows the compile up to `MEMORY_COMPILE_WAIT_SECONDS` (default 30 min); a compile still running is deferred, not failed |
| 29.4 | generations never pruned (1 GB a day) | high | nightly runs `prune_generations.py --apply` after the index step |
| 22 | provider/model chosen at install not persisted to hooks env or units | high | installer writes `MEMORY_LLM_PROVIDER` / `MEMORY_CLAUDE_MODEL` where it writes the roots |
| 20 | deleting a project directory blocks its checkpoints for ever; no rebuild command | high | `project_journal.py --rebuild <slug>` from committed checkpoints; the vault root is never a project slug |
| 28 | compile drops claims on the quote rule silently; ok hides 16 drops | high | log each drop with the quote and nearest line; expand a unique substring to its line instead of dropping; count in state and log |
| 18 | uv must be exactly 0.12.3; `uv self update` fails for tool installs | medium | the failure names both upgrade paths; the exact pin stays until a research note says otherwise |
| 23.1–5 | Codex conflict wording, pyright identity, transcript allowlist, rebuild output, health budget | medium | one small change each |
| 29.1 | vectors absent after installing the extra; doctor hides it; USER-GUIDE wrong | medium | doctor reads `vector_state` from the manifest without the budget; `install.sh --with-semantic`; guide corrected |
| 26.1 | an old active generation hides a freshly published note; empty envelope loses the trace | medium | envelope keeps the selected generation and freshness on empty results; freshness contract documented |
| 26.2–3, 29.5 | quarantine outcome, doctor distinctions, claim `evidence_unresolved` reasons | medium | outcomes named in compile output and state; doctor names the reason |
| 19 | shipped example knowledge treated as the user's memory | medium, design | published example pages carry `published_example: true` and stay out of guardrails and default recall |
| 29.2 | recall corpus is 92% the checkout's own code and docs | medium, design | default recall corpus is `knowledge/`; code and docs only under an explicit profile |
| 29.3 | RU→EN recall fails with e5-small | research | done: `bge-reranker-v2-m3` is the default reranker at depth 10, on every question, resident in the MCP server (xMRR 0.60 → 0.98; encoder swaps at most +0.04) |
| 25 / PR 27 | Codex recursion, wrong V3 dispatch, false hook health | high | review PR 27 against the gates, merge onto `work` |
| 24 | roadmap to replace codebase-memory-mcp | roadmap | acknowledged; acceptance criteria adopted into the plan, not fixed here |

## Status at the end of 2026-09-10

Done on `work-rounds`, each with a test: 17, 18 (message), 20, 21, 22, 23.1–5,
26.1, 26.2, 26.3, 28, 29.1, 29.4, 29.5, 29.6, PR 27 merged. Notes:
`2026-09-10-an-empty-answer-still-names-its-index.md`,
`2026-09-10-an-outcome-is-named-not-summarised.md`,
`2026-09-10-health-is-measured-at-night-and-read-in-the-morning.md`.

Waiting for the owner's decision (research written, no code):

- 19 and 29.2 — `2026-09-10-whose-memory-is-this.md`: mark published pages,
  hide them on a fresh vault, default `recall` reads `knowledge/`.
- 29.3 — `2026-09-10-a-question-in-russian-and-a-note-in-english.md`: fixture
  and measurement first; a model change only with the numbers.
- 18 — the exact `uv` pin stays; loosening it needs its own note.
- 24 — acknowledged; the acceptance criteria go into the code-navigation plan.

Later on 2026-09-10 the owner asked whether the fixes were shortcuts; four
were and are replaced (`2026-09-10-four-shortcuts-replaced.md`): contention by
exception type and code, batch outcomes returned not collected globally, the
search reporting its own trace to the envelope, and tests that adopt a vault
for real and spawn the probe's descendant deterministically. Real adoption
found two more doctor defects on adopted vaults (queue migration, repair),
fixed with tests. PR 27's tests are made platform-true for Windows CI.

Decided by the owner later on 2026-09-10 and done: 19 — the repository ships
no memory (`2026-09-10-the-repository-ships-no-memory.md`); 29.2 — the vault
generation holds `knowledge/` only (`2026-09-10-the-memory-index-holds-memory.md`).
29.3 was measured the same day on a 45-query cross-lingual corpus
(`2026-09-10-cross-lingual-memory-world-practice.md`, reports under
`benchmark/results/crosslingual-2026-09-10/`): with the current
`multilingual-e5-small`, cross-language MRR is 0.60; with
`BAAI/bge-reranker-v2-m3` over its top ten it is 0.98; replacing the encoder
alone gains at most 0.04. Proposed to the owner: keep the encoder, make the
reranker the default at depth 10, resident in the MCP server. The owner said
yes the same day and it is done (`CHANGELOG.md`, Unreleased → Changed).
Open, found on the way: nothing in the product downloads model weights, for
the encoder or the reranker — a fresh install answers lexical-only until the
two models are in its Hugging Face cache. Model acquisition at install is
the next item and needs its own research note.
Seen while measuring the reranker on the live vault on 2026-09-10 and fixed
the same day (`2026-09-10-a-generation-outlives-its-extractor.md`): a warm
`search` took 34–49 s and answered lexical-only, empty for Russian, because
the 2026-09-08 chunker change made every earlier generation "semantically
invalid" under the read path's content check and the refusal was re-derived
six times per search. Older generations are now served after a structural
check, and a refusal is remembered.
