# Итог 2026-09-11: аудиты 2026-09-10 и issues на GitHub

Проверено на коммите `dc1d12f`. Он вошёл в `main` через PR #30, слитый
2026-09-11 в 20:29 UTC. PR с номером 50 в репозитории нет. Прогон GitHub
Actions на этом коммите (run 34639876448) зелёный: 53 из 53 заданий, включая
Windows.

## Аудиты 2026-09-10: 54 находки, открытых нет

У каждой находки в документе аудита есть строка `Status` со ссылкой на
датированное исследование. Скрипт проверил, что все упомянутые исследования
лежат в `main`.

**Эксплуатация и надёжность**
(`docs/AUDIT-2026-09-10-operations-and-reliability.md`), OPS-01…OPS-23:

- 18 исправлены: OPS-01…OPS-13, OPS-16, 18, 20,
  21, 23.
- 3 закрыты: OPS-14 (установщик и чистая установка проверяются в CI на всех
  трёх системах), OPS-15 (правило 5), OPS-17 (два теста проверяют намерение,
  а не дефект).
- OPS-19 закрыта исправлениями OPS-07 и OPS-08. Я перечитал её: живого или
  неизвестного владельца блокировки ждут при любом её возрасте.
- OPS-22 закрыта частично, по решению: токен владельца компиляции больше не
  хранится в глобальной переменной. Охраняемые глобальные переменные сервера
  MCP оставлены сознательно.

**Память и поиск** (`docs/AUDIT-2026-09-10-memory-and-retrieval.md`),
H1–H5, M1–M13, L1–L13:

- 22 исправлены или удалены: H1, H2, H4, H5, M2–M6, M9–M11, L1–L3, L5–L7,
  L9–L12.
- 7 закрыты: H3, M7, M8 (реальные границы чтения названы), M12, M13, L8, L13.
- 2 после проверки оказались не дефектами. M1: на рабочем пути поиска срок в
  необязательные стадии не передаётся. L4: в строке стоит настоящее тире, а не
  «???».

Сверх аудита весь старый продуктовый код в `scripts/` и `benchmark/` приведён к
правилу 5. Обе проверки сложности показывают ноль нарушений.

**Вопросы, на которые аудиторы не ответили (9).** Два отвечены попутно:
вопрос Q2 про поиск совпадает с M1, вопрос Q4 закрыт вместе с H2. Остальные
семь разобраны позже в тот же день
(`docs/research/2026-09-11-the-seven-questions-the-audits-left-open.md`):

- **эксплуатация Q1**, убитая компиляция: была дефектом, ночной проход считал
  такую ночь успешной. Исправлено: компиляция, которая началась в эту ночь и
  не записала итог, теперь считается сбоем;
- **Q2**, Windows и другой контекст безопасности: не дефект. Отказ в доступе
  значит «неизвестно», а это ожидание, не кража блокировки;
- **Q3**, убийство во время активации поколения: не дефект. Активация — одна
  транзакция SQLite;
- **Q4**, у Codex нет захвата подсказок и правок: Codex эти события
  поддерживает, их просто не подключили. Подключение — отдельный шаг;
- **Q5**, где помощники плагина: в `integration_adapter.py` и
  `codex_memory.py`;
- **память Q1**, цена перечитывания заметок: 47–104 мс на живом хранилище.
  Не проблема;
- **память Q3**, записи сессий затирали друг друга: была скрытым путём потери
  (без идентификатора, с заменёнными символами, длиннее 64 символов).
  Исправлено: такие имена получают хеш.

## Issues на GitHub

**Закрыты 10 issues:** #17, #18, #19, #20, #21, #22, #23, #25, #26, #28.
PR #27 слит.

**Открыты 2.**

**#29, семантический поиск на установленном хранилище.** По
`docs/ISSUES-2026-09-10.md` и коду сделаны все шесть пунктов, поэтому issue
можно закрывать:

- 29.1: установщик скачивает веса (`scripts/install_models.py`), `doctor`
  читает `vector_state` из манифеста;
- 29.2: корпус поиска хранилища — только `knowledge/`;
- 29.3: русские вопросы к английским заметкам. Реранкер
  `bge-reranker-v2-m3` поднял MRR с 0,60 до 0,98 на 45 запросах;
- 29.4: ночной проход удаляет лишние поколения;
- 29.5: `doctor` называет причину по каждому утверждению;
- 29.6: мелочи.

**#24, дорожная карта замены codebase-memory-mcp.** Сделаны раздел A (тёплые
ответы, фоновое обновление), бо́льшая часть B (поиск символов, исходный код по
имени, трассировка с глубиной, покрытие, влияние изменений), раздел C
(подсказки при поиске и напоминания для Claude Code, Codex и OpenCode) и D1
(рабочие копии индексируются и удаляются).

Доделано вечером 2026-09-11:

- **B, трассировка `data_flow` и `cross_service`**: извлечение пишет по
  каждому разрешённому вызову, какое имя вызывающего ложится на какой параметр
  вызываемого, и связывает клиентские HTTP-вызовы с узлом маршрута. Режим
  `data_flow` идёт по этим связям шаг за шагом, режим `cross_service` — по
  вызовам и через маршрут в обработчик, который его объявляет. Ответ сам
  называет себя связыванием аргументов, а не анализом потоков данных.
- **D2, связи между репозиториями**: файл подсказок каждой рабочей копии
  получил таблицу маршрутов. Маршрут, которого нет в своём репозитории,
  ищется среди маршрутов всех остальных проиндексированных копий; такой шаг
  называет обработчик, файл и репозиторий и дальше не идёт.
- **E, набор задач**: `benchmark/code-parity-v2.json` — прежние 13 задач плюс
  две про связывание аргументов и одна про то, какие тесты покрывают функцию;
  кросс-сервисные вопросы — в отдельном наборе на фикстуре из двух
  репозиториев. Эталонные ответы выверены по коду вручную. Прогон — только с
  вашего разрешения.

Доделано ночью 2026-09-12, после вашего «ставь»:

- **B, точная навигация для Go**: `gopls v0.23.0` отвечает для `.go`
  определения, ссылки, реализации, типы, вызовы и всплывающую справку —
  проверено на живом Go-модуле: 0,48 с на холодную, 0,10 с на тёплую, ответы
  подтверждены сервером. Установка — одна команда, 44 с и 324 МБ; gopls
  собирается из исходников пинованным компилятором Go 1.27.1, потому что
  готового бинаря авторы не публикуют. Пока не установлено, Go отвечает как
  раньше, по структуре. Разбор:
  `docs/research/2026-09-12-installing-go-and-building-gopls.md`.

Осталось:

- **B, язык запросов к графу** (аналог Cypher): уже закрыт типизированным
  запросом `mode=query`, отдельный язык не нужен.
- **Прогон стенда** (`code-parity-v2` и кросс-сервисный набор): ждёт вашего
  прямого разрешения.
- **Rust** (не из issue #24, ваш вопрос): rust-analyzer публикуется готовым
  бинарём, но без cargo и исходников стандартной библиотеки он видит проект
  лишь частично. Цена набора — архивы rustc 80 МБ, rust-std 31 МБ, cargo
  12 МБ, rust-src 6 МБ и сам сервер ~15 МБ, все с контрольными суммами от
  rust-lang и без компиляции. Ждёт вашего решения.

## Открыто вне аудитов и issues

- **Живое хранилище.** По сводке здоровья на начало сессии 2026-09-11 ночное
  обслуживание не проходит с 2026-09-07. Там же: одна отказанная попытка
  транзакции и четыре потерянные записи сессий. В этом отчёте не разбиралось.
- **Карантин.** 102 спорных утверждения ждут правила, как с ними поступать.
  Решение за владельцем.
- **Правило 5 в тестах.** 1 113 нарушений, в основном потому что проверка
  считает каждый `assert` ветвлением.
- **Задержка.** Чтение графа ссылок на живом хранилище занимает около 1,8 с на
  вызов. Замечено, не исправлялось.
