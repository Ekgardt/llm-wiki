# A reflection is checked before it is written

Dated 2026-09-17. Finding M-A1 of the third audit (high, reproduced). The research before the
fix.

## What was found

- `reflection.reflect_page` writes whatever the provider replied as the new body of a page.
  Nothing checks the reply. Reproduced by the audit: a refusal sentence became the whole body
  of a page, its `One-sentence summary:` line was lost (the page then drops out of the
  guardrails, the index summary and the L0 tier), and the original survived only inside the
  `<details>` of `## History`.
- The weekly pass (`scheduled_weekly.py`) runs this unattended with `apply=True` on every
  candidate.
- There is no page-type filter: a `type: decision` page is rewritten in place, against the
  vault's own rule — "Decisions are immutable: supersede, never edit in place" (`CLAUDE.md`
  §4, rule 12).
- The prompt asks the model to append a `## History` section holding the original body, and
  `_reflected_page` appends the original body again: every reflected page carries its old
  body twice.
- `_retired_or_reflected` skips any page containing `## History`, so a page reflected once
  is never a candidate again however many updates it gathers later, and a hand-written page
  with such a heading is never reflected at all. Counting `## Update` sections over the
  whole text would also count the ones preserved inside the history block.

## Practice on this date

- An unattended writer validates a generated artefact against invariants of the thing it
  replaces before it commits, and keeps the old one on any doubt. OWASP names the failure
  "insufficient validation, sanitization, and handling of the outputs generated by large
  language models before they are passed downstream", and its first remedy is to "treat the
  model as any other user, adopting a zero-trust approach, and apply proper input validation
  on responses coming from the model to backend functions"
  ([OWASP LLM05:2025 Improper Output Handling](https://genai.owasp.org/llmrisk/llm052025-improper-output-handling/)).
- A-MEM's memory evolution, which this pass implements, rewrites notes from existing text
  only; a rewrite that loses the note's own identifying fields is outside that operation
  ([A-MEM, arXiv:2502.12110](https://arxiv.org/abs/2502.12110)).

## The decision

- A `type: decision` page is never a reflection candidate.
- The rewrite is accepted only if it keeps what the page is known by: its
  `One-sentence summary:` line when the original had one, no `## Update (` section left (the
  purpose of the pass), and a body of at least `MIN_REFLECTED_WORDS` words. Otherwise the
  page is left untouched and the line says why.
- The prompt no longer asks the model for a history section; the pass appends the one
  history block itself, so the old body is stored once.
- A page is judged by its live body — the text before the pass's own
  `## History (pre-reflection` marker — so a reflected page that gathers new updates is
  reflected again, and a hand-written `## History` heading does not hide a page.

Files: `scripts/reflection.py`, `tests/test_reflection.py`,
`docs/research/2026-09-17-a-reflection-is-checked-before-it-is-written.md`.
