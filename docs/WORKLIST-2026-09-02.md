# Всё, что надо сделать — один список, 2026-09-02

Порядок — по важности, а не по разделам. Обоснование каждого пункта в
`docs/research/`; проверка плана на «даст ли скачок» —
`docs/research/2026-09-02-will-the-plan-give-a-jump.md`.

Главный вывод проверки: **ни один инженерный пункт скачка не даёт.** Крупнейший
измеренный рычаг в опубликованном разборе — глубина извлечения, плюс 4.2%;
нарезка на куски, которую я вчера сделал, — плюс 0.8%. Скачок даёт только одно:
политика отказов, и это решение, а не задача.

---

## Сейчас теряются данные

**1. Захваты сессий не сохраняются.** 452 записи в журнале потерь, последняя
сегодня в 08:29, `FileNotFoundError`. Это память, которая не доезжает до
хранилища. Важнее всего остального в списке.

**2. `run/` растёт квадратично** — 4.9 ГБ за две недели, потому что каждая
дозапись журнала кладёт копию до и копию после. Внутри тридцатидневного
контракта отката, поэтому это ваше решение, а не мой баг.

---

## Точность ответов

**3. Доделать нарезку сессии на куски.** Написано и один раз измерено: 0.2750 →
0.3229 по судье, межсессионный разряд 0.02 → 0.19. Правило блокирует из-за
просадки в «обновлении знаний». Осталось два прогона и разбор просадки.

**4. Проверить две настройки: шире окно ответа и сильнее модель-ответчик.**
Глубина извлечения — крупнейший рычаг в разборе. Плюс это говорит, узкое место
вообще в памяти или нет. День работы, и от результата зависит порядок дальше.

**5. Затухающая связь между сущностями** (приём мухоловки). Встретились рядом —
подняли друг другу вес, подъём спадает. Бьёт в межсессионные вопросы, лишних
обращений к модели нет.

**6. Перечитывать собственные следы поиска** (приём обученного иммунитета).
Данные уже пишутся, читающего слоя нет.

**7. Включить реранкер.** Сейчас в обычной установке он не запускается вовсе:
нужны две переменные окружения, глубина 20, срабатывает по условию. В поле это
+5…15 пунктов точности ранжирования и 100–300 мс на процессоре.

**8. Вычисленный «как есть сейчас»** поверх накопительного хранилища — для
разряда «обновление знаний», самого слабого у накопительных схем.

**9. Факты со временем при записи** (приём сойки). Понижен в приоритете: разбор
ставит изменения на стороне записи ниже, чем на стороне чтения. Отдельное
решение до кода.

**10. Память о неверном** (приём CRISPR). Сначала сохранять то, что ворота
выбрасывают — сейчас 28 готовых ответов из 200 уничтожаются, и мы не знаем,
были ли они верны.

---

## Измерение

**11. Мерить на стенде, который учитывает отказы.** LongMemEval считает
молчание ошибкой, а в поле прямо сказано: оценки, не оценивающие отказ,
поощряют ровно то поведение, против которого системы и строят. Есть
`LIT-RAGBench` с отдельным разрядом отказа и `RefusalBench`. Это меняет число,
не меняя систему, и это честнее.

**12. Запустить LoCoMo и BEAM.** Два из трёх стендов, которыми меряется поле,
у нас не запускались ни разу.

---

## Вторая половина продукта, о которой в плане не было ни слова

**13. Навигация по коду.** Мы заменяем codebase-memory-mcp и Graphify. По
токенам 3.82× — сравнялись почти, но цель обойти. Отдельного плана нет.

**14. Проверить качество навигации так же, как проверяем память** — своим
стендом с правилом, а не на глаз.

---

## Ждёт вашего решения

**15. Политика отказов.** Пять вариантов с ценой —
`docs/OPTIONS-grounding-vs-coverage-2026-09-02.md`. Единственный рычаг размером
со скачок: сейчас охват 33% при точности 0.77 там, где отвечаем.

**16. Расход токенов против точности** в пункте 4.

**17. Форма хранилища** в пункте 9.

**18. Квадратичный след отката** в пункте 2, срок жизни резервирования и две
находки из аудита.

---

## Чего в списке нет намеренно

Памяти в весах модели — поле говорит, что это ломает послушание модели, а наша
беда знание, а не стиль. Отказа от поиска — без найденных доказательств любая
модель на этом наборе даёт меньше четырёх процентов.

---

## Where the losses actually are — measured 2026-09-02, n=200 seed 101

Read off `benchmark/longmemeval-claimlevel-n200-r1.json` and its judged rows,
using the **labelled** answer session rather than the substring flag.

**Retrieval is not the bottleneck.** Of 186 questions that have a labelled
answer session, retrieval brought it back for 162 (87%), ranked first in 143 of
them. Almost every loss happens after the right evidence is already in hand.

**87 questions outside the abstention category produced no answer. 62 of them
had the answer session retrieved.** Their breakdown:

| cause | count | status |
|---|---|---|
| one unresolvable citation killed the document | 18 | fixed in `772cbf0`, unmeasured |
| provider replied with a valid JSON fence plus trailing prose | 15 | open — 13 of the 15 are recoverable |
| refused despite having the evidence | 22 | open — needs the prompt looked at |
| conflicting / time-scope / schema | 7 | open |

### Next two items, in order

1. **Accept the first fenced JSON block anywhere in the reply.**
   `_unfenced` in `scripts/query_memory.py` only unwraps a response that is
   *exactly* one fence. Measured: 15 complete, correct answers were discarded
   for having a sentence of commentary after the closing backticks; 10 parse
   from a leading fence and 3 more from a fence that follows prose. Every claim
   is still gated afterwards, so trailing prose is discarded, not trusted.

2. **Find out why 22 questions refuse with the evidence retrieved.** No
   hypothesis yet. The reply text is recorded in `raw_reply`, so this is
   readable rather than guessable.

### A measurement caveat that must not be repeated

`gold_in_prompt` is a substring test for the gold answer inside the prompt. On
the answered rows it reads False for 39 questions of which **29 were judged
correct** — it misses roughly three quarters of the cases where the evidence was
in fact present. It is a lower bound and nothing more. The size of the
"retrieval missed it" bucket must be taken from the labelled answer session, not
from that flag. This is the fourth defect of this family; see
`docs/research/2026-09-01-a-category-graded-by-the-wrong-question.md`.

### Status 2026-09-02, later the same day

Done and committed:

- `772cbf0` — an unresolvable citation is dropped, not fatal (18 questions).
- `ae43ee6` — the first fenced JSON block is read wherever it sits, so a
  complete answer is not discarded for a sentence of commentary (13 of 15).
  Full suite green: 7920 passed, 256 skipped.
- `docs/research/2026-09-02-how-much-evidence-is-too-much.md` — three
  independent results saying 117 000-token prompts are far past the point where
  more evidence helps.

In flight: a window sweep at n=50, seed 101, candidates fixed at 40, answer
budget 122 880 / 32 768 / 12 288. Retrieval is held constant so only what
reaches the model changes.

Deliberately **not** done yet: the three 200-question runs of the shipped
configuration. Measuring a setting twice while still changing it wastes hours;
the three runs happen once the window is settled.

---

# Состояние на 2026-09-05

## Сделано и отгружено

**3. Нарезка сессии на куски** — отгружена. Плюс найден и исправлен дефект,
который она внесла: кусок резался посреди буквы UTF-8 и убивал сборку индекса.

**4. Две настройки проверены.** Свип по ширине окна: 122880 — пик. Уже 32768
теряет и полноту, и точность (0.54 → 0.36 → 0.16), выше 122880 кривая плоская.
Опасение «окно слишком широкое» опровергнуто нашими же данными.

**7. Реранкер включён** — работает в живом хранилище,
`signals_used: [lexical, dense, reranker]`.

**9. Факты со временем при записи** (приём сойки) — отгружено. Относительные
даты в репликах пользователя разрешаются в момент записи и становятся обычным
цитируемым текстом. Плюс календарь событий: запись несёт день и предложение, а
вопрос несёт даты, которые он лишь подразумевает.

**18. Квадратичный след отката** — закрыт сжатием. 5.3 ГБ, образы сжимаются до
9,7%. Хеш остался хешем несжатых байт.

**Сверх списка, по результатам замеров:**
- Ворота больше не уничтожают ответ целиком; одна нерезолвящаяся ссылка стоила
  18 вопросов из 200.
- Модель называет только номер ссылки, локатор берём из своего манифеста.
- Готовый ответ не выбрасывается из-за фразы после блока JSON — 13 из 15.
- Поиск перестал требовать совпадения **всех** слов вопроса: «в какой день
  недели у меня занятие» находило ноль.
- **Утверждение может быть вычисленным** — сумма, счёт, разница дат. Семь из
  восьми неверных ответов требовали именно этого. Замер: правильных 27 → 33,
  неверных 8 → 4.
- Индексу разрешено отставать, поэтому он вообще существует; устаревший текст до
  модели не доходит.
- Журнал проектов перестал быть пустым (было 3713 событий без содержания).
- Провалившееся ночное обслуживание видно на старте сессии.

## Осталось

**1. Захваты сессий.** 446 потерь, счётчик живой. Не закрыто.

**5. Затухающая связь между сущностями** (мухоловка). Не начата. Приоритет
понижен замером: извлечение находит нужную сессию в 87% случаев, узкое место не
там.

**6. Перечитывание собственных следов поиска** (иммунитет). Не начата.

**8. Вычисленный «как есть сейчас»** — частично снят вычисляемыми утверждениями
(`latest`), но отдельного слоя нет.

**10. Память о неверном** (CRISPR). Повод снят — ворота больше не уничтожают
ответы, — механизм не сделан.

**11. Стенд, учитывающий отказы** (LIT-RAGBench, RefusalBench). Не начато.

**12. LoCoMo и BEAM.** Не запускались.

**13-14. Навигация по коду.** Ни плана, ни стенда. По токенам 3.82× против cbm.

**15-17. Решения за владельцем:** политика отказов, размен токенов и точности,
форма хранилища для фактов со временем.

**Главный долг измерения.** Разброс стенда на 50 вопросах — 0.08, поэтому все
частные приросты этого дня внутри шума. Честной цифры на 200 вопросах в трёх
прогонах до сих пор нет; последний одиночный прогон дал 0.5750 против базы
0.2133.
