# knowledge/inbox/

Unprocessed staging. Capture here first; after compile + citation, move trusted sources to `knowledge/raw/`.

Scripts create subdirs on demand (e.g. `articles/`). No `.gitkeep` required.
