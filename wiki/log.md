# Wiki Log

- 2026-04-30 — Added Codex compatibility for the global LLM-wiki memory harness without forking the backend: (a) new `scripts/codex_memory.py` wraps the existing SessionStart/SessionEnd/lookup scripts so Codex reuses the same slug resolution, `wiki/projects/<slug>/state.md` files, and shared `memory/daily/` breadcrumbs as Claude Code; (b) populated `~/.codex/AGENTS.md` with always-on instructions to pull per-project state, answer from the curated wiki, and write durable knowledge back into the same vault; (c) added user-level skill `~/.codex/skills/llm-wiki-memory/SKILL.md`; (d) documented the integration in [[wiki/projects/llm-wiki/codex-integration|LLM-wiki — Codex integration]]. Important limit recorded explicitly: Codex does not expose Claude-style hooks, so the backend is shared but the trigger surface is instruction-driven rather than hook-driven.
- 2026-04-11 — Initialized wiki structure and operating conventions.
- 2026-04-12 — Compiled Karpathy X thread: added [[LLM Knowledge Base]], [[Andrej Karpathy]], [[Obsidian]], [[Karpathy LLM Wiki Workflow]].
- 2026-04-12 — Knowledge-review cleanup: removed empty "Comparisons" section from [[index]], expanded [[Vault Home]] into a proper front door, added "Monitoring & Metrics" and review cadence to [[Review Workflow]].
- 2026-04-12 — Applied knowledge-review fixes: added `Source:` to [[Ingestion Workflow]], [[Retrieval Workflow]], [[Review Workflow]]; standardized their `Related` sections; trimmed [[Andrej Karpathy]] to a pure entity page and made [[Karpathy LLM Wiki Workflow]] the canonical pipeline page; compacted [[Obsidian]] to reduce overlap with [[LLM Knowledge Base]]; strengthened [[Vault Home]] callout in [[index]].
- 2026-04-13 — Added durable raw-source page [[Karpathy X Thread - April 2026]] under `wiki/raw-sources/` to eliminate single-point dependence on the external X post. Updated `Source:` blocks on all derived pages to cite the new wiki record alongside the captured `inbox/` copy. Added `## Editorial note` to [[index]], [[Vault Home]], and [[log]] to mark them as vault metadata rather than source-derived content. Registered a "Raw sources" section in [[index]].
- 2026-04-13 — Added comparison page [[Wiki vs Memory Compiler vs Fusion]] under `wiki/comparisons/`, weighing the Karpathy wiki approach against the claude-memory-compiler approach and this vault's fusion of the two. Registered a "Comparisons" section in [[index]]. Characterizations of the memory-compiler and fusion approaches are flagged **preliminary** — inferred from project operating instructions rather than a `raw/` source.
- 2026-04-13 — Promoted `memory/knowledge/patterns/mirror-existing-pipelines` (+ its concept counterpart) into the wiki as [[Pipeline Mirroring]] under `wiki/concepts/`. Added reciprocal links from both memory pages, registered in [[index]] under Concepts. Sibling to [[Preliminary Flagging]] — both are vault conventions surfaced by the 2026-04-13 memory audit session.
- 2026-04-13 — Promoted session-memory decision into the wiki as [[Preliminary Flagging]] under `wiki/concepts/`. Origin: `memory/knowledge/decisions/flag-inferred-content-as-preliminary.md`. Added reciprocal link from the memory decision to the new wiki page, and registered [[Preliminary Flagging]] in [[index]] under Concepts. This is the first `bridge-promote-insight` pass — memory → wiki.
- 2026-04-13 — Executed step 1 of [[Memory Subsystem Action Plan]]: extended `memory/operating-model.md` with a `## Compile Procedure` section covering when to run `/session-memory-compile`, what to lift from `memory/daily/`, distinctions between concepts/decisions/patterns/debugging/qa, what not to lift, how to update `memory/index.md` and `memory/log.md`, and the `memory/` vs `wiki/` promotion boundary via `bridge-promote-insight`.
- 2026-04-13 — Audited `memory/` subsystem: `daily/` working, `knowledge/{concepts,decisions,patterns,debugging,qa}` all empty, no compile procedure defined in `operating-model.md`. Added [[Memory Subsystem Action Plan]] under `wiki/syntheses/` with current-state findings and next steps (extend operating model, run first compile pass over 2026-04-13 daily, clarify `memory/` vs `wiki/` boundary). Registered in [[index]] under Syntheses.
- 2026-04-14 — Colleague-review pass (runtime/vault separation): (1) moved runtime OUT of vault per "Priority 1" — `memory/state/` and `reports/memory/` now live in `$LLM_WIKI_STATE_ROOT/memory-state\` and `$LLM_WIKI_STATE_ROOT/memory-reports\`, overridable via `LLM_WIKI_STATE_ROOT` env var. Obsidian and any future git remote no longer track ephemeral hashes, dedupe timestamps, lint reports, or debug dumps. (2) Killed "no-op compile noise" in `memory/log.md` — `compile_memory.py` now only appends when a pass actually touched a knowledge page; pure heartbeat compiles are recorded in `state.json` only. (3) Deleted dead code: `scripts/postcompact_capture.py` (script was not registered in `settings.json` hooks) and the empty `hooks/` directory. (4) Added `outputs/README.md` placeholder so the directory stays referenced from CLAUDE.md. (5) Extended lint's `BROKEN_LINK_SKIP_NAMES` with `log.md` — append-only changelogs may carry literal `[[...]]` examples in historical entries that can't be rewritten. Documentation in `README.md`, `memory/AGENTS.md`, and 3 skill files updated to reflect the new `$LLM_WIKI_STATE_ROOT` convention.
- 2026-04-14 — Backlink + sparse content pass, lint reduced to **0 findings**: (1) added 15 reciprocal backlinks with short connection-rationale blurbs across 9 pages — every A→B link now has a matching B→A link; (2) expanded 9 sparse pages past the 200-word floor with real content, not filler — each addition is a genuinely new subsection (Variants/Prevention for the Edit debugging page; "Why dating matters" + checklist for the audit pattern; "Why his framing matters" for Karpathy entity; "Why Obsidian is the frontend" for Obsidian entity; fleshed-out steps for Ingestion/Retrieval/Review Workflow; "Why this page exists as a stub" for the pipeline-mirroring concept redirect; "When NOT to apply" + failure-mode section for mirror-existing-pipelines pattern); (3) reworded one debugging example to remove a literal `[[X]]` that was being flagged as a broken wikilink by its own lint. Vault now passes all seven structural checks with zero findings.
- 2026-04-14 — Post-audit cleanup (5 seam closures): (1) created `wiki/qa/README.md` placeholder — the directory now exists as referenced from `README.md` pipeline diagram and `/knowledge-qa-file-back` skill; (2) bumped `compiled_daily_hashes['2026-04-13.md']` in `memory/state/state.json` to match current file hash (hygiene pass only removed test entries — no new durable content to lift — so no actual compile run was needed; `stale_compiled` lint finding cleared); (3) cleaned test/unknown/auto-test keys from `state.json::flush_dedupe`; (4) extended `/bridge-promote-insight` skill with explicit `## Promoted to wiki` contract — memory origin page MUST carry the marker + reciprocal wikilink, step 5 now states "a one-way promotion marker is a bug"; (5) documented `--trigger auto|manual` parameter and SHA-256 incrementality in `scripts/compile_memory.py` docstring.
- 2026-04-14 — Priority-4 scale prep: rewrote `/knowledge-lookup` skill as an explicit three-tier retrieval strategy (DIRECT <50 / HYBRID 50–300 / QMD >300) that matches the scale range Karpathy described and coleam00 acknowledged as the RAG breakpoint. Added `scripts/lookup_mode.py` — counts curated wiki pages (excluding editorial metadata), recommends the tier, and inspects QMD index freshness by reading `$LLM_WIKI_STATE_ROOT/qmd\index.sqlite` mtime directly (robust against the Windows cmd.exe / bash PATH asymmetry where `qmd` is only resolvable in Git Bash). Current vault: 14 curated pages → DIRECT tier is correct. QMD infrastructure already in place (5 collections, 21 docs, 59 vectors) — will earn its keep when the vault crosses 50 pages. Documented in `README.md` Automation section.
- 2026-04-14 — Priority-3 feature pass: (a) added `/knowledge-qa-file-back` skill — promotes a just-answered question into either `wiki/qa/` or `memory/knowledge/qa/` using a two-question routing checklist; (b) added `/contradict-check` skill — wraps `lint_memory.py --contradictions` with instructions for installing as a git pre-commit hook; (c) rewrote top-level `README.md` with a full pipeline diagram showing the wiki / memory two-layer architecture, hook flow, auto-compile trigger, and skill inventory; (d) added an explicit two-question decision table to `memory/operating-model.md` for the `memory/` ↔ `wiki/` boundary, replacing the prior bullet-list rule with a checklist + worked examples.
- 2026-04-13 — Borrowed infrastructure patterns from `coleam00/claude-memory-compiler`: (a) introduced `wiki/connections/` as a first-class article type with first entry [[2026-04-13 Three Conventions One Root|Three Conventions, One Root — 2026-04-13]] — a cross-concept insight that [[Editorial Notes Pattern]], [[Preliminary Flagging]], [[Pipeline Mirroring]] are three orthogonal operationalizations of CLAUDE.md rule 6; (b) seeded `memory/knowledge/qa/` with its first entry `inbox-vs-raw-after-compile` (settled Q about post-compile file location); (c) registered a "Connections" section in [[index]].
- 2026-04-13 — Borrowed infrastructure patterns from `coleam00/claude-memory-compiler`: (a) moved `inbox/articles/Thread by @karpathy.md` → `raw/articles/` (already compiled; inbox should stage unprocessed material only) and updated `Source:` citations across seven wiki pages + the raw-source record; (b) unified summary convention — replaced `## Summary` headers with `One-sentence summary:` inline on [[LLM Knowledge Base]], [[Andrej Karpathy]], [[Obsidian]], [[Karpathy LLM Wiki Workflow]], [[Wiki vs Memory Compiler vs Fusion]] per CLAUDE.md wiki-page conventions; (c) promoted `memory/knowledge/concepts/editorial-notes-pattern` into the wiki as [[Editorial Notes Pattern]] under `wiki/concepts/`, with reciprocal `## Promoted to wiki` marker on the memory page; (d) deduplicated `memory/knowledge/concepts/pipeline-mirroring` against the sibling pattern — concept reduced to a redirect stub, pattern remains canonical (tiebreaker: patterns > concepts).

- 2026-04-18 — Revised [[Memory Subsystem Action Plan]] end-to-end: marked all four action items done with dates and follow-up context; resolved all three open questions (manual/auto → hybrid auto-compile after 18:00 with manual fallback; qa/decisions overlap → kept separate via patterns>concepts tie-breaker; daily pruning → never delete, archive compiled logs >90d when directory passes ~30 files). Added `## Editorial note` footer to `memory/log.md` to bring it to parity with `memory/index.md`, `wiki/index.md`, `wiki/log.md`, and `wiki/Vault Home.md` as vault metadata. Action Plan now documents two dormant follow-ups: `scripts/archive_daily.py` (write when threshold crossed) and periodic re-review of the plan when the subsystem grows.
- 2026-04-19 — Drafted [[Global Multi-Project Migration Plan]] under `wiki/syntheses/`: five-phase plan for extending the vault from single-project to a global "second brain" that auto-captures and restores context across every Claude Code session. Phase 0 decisions recorded (slug rule = parent folder name; project vs cross-cutting boundary; secrets hygiene; auto-commit yes / auto-push no). Registered in [[index]] under Syntheses. Phases 1–5 pending; execution begins next.
- 2026-04-19 — Executed Phase 1 of [[Global Multi-Project Migration Plan]]: (a) created `wiki/projects/` with `_template/state.md` skeleton and README; (b) migrated LLM-wiki as first project at `wiki/projects/llm-wiki/state.md`; (c) registered new "Projects" section in [[index]]; (d) added `state.md` to `lint_memory.py::EDITORIAL_NAMES` (per-project state pages are auto-updated metadata, same rationale as `index.md`/`log.md`); (e) updated `scripts/lookup_mode.py` to exempt `state.md`/`README.md` and skip `projects/_template/` from the curated-page counter; (f) audited `/knowledge-lookup` skill — no hard-coded section paths, only refreshed the page-count footnote; (g) added reciprocal backlinks from the new plan to [[Memory Subsystem Action Plan]], [[Pipeline Mirroring]], [[Preliminary Flagging]], [[Wiki vs Memory Compiler vs Fusion]]. Lint: 1 transient finding (uncompiled daily), 0 structural issues. DIRECT tier retained at 15 curated pages. Phase 2 (global `~/.claude/` layer) is next.
- 2026-04-19 — Executed Phase 2 of [[Global Multi-Project Migration Plan]] (**live-verified**, entirely outside the vault — home-level files are machine-local, not git-tracked): (a) added `LLM_WIKI_ROOT=$LLM_WIKI_ROOT` to `~/.claude/settings.json::env`; (b) created `~/.claude/CLAUDE.md` with slug rule (parent folder name, lowercase, hyphens), project vs cross-cutting boundary, session start/end contract, and secrets hygiene; (c) copied `knowledge-lookup` to `~/.claude/skills/` and rewrote paths to absolute `$LLM_WIKI_ROOT/...` so it works from any cwd; (d) deliberately did NOT copy the other 7 vault-maintenance skills (`bridge-promote-insight`, `contradict-check`, `knowledge-compile`, `knowledge-qa-file-back`, `knowledge-review`, `session-memory-compile`, `session-memory-review`) to user-level — they only make sense inside the vault, per 2025-era best-practice separation (option A from the five-alternative analysis). Verified from `<test-folder> `/knowledge-lookup` correctly read `wiki/concepts/Pipeline Mirroring.md` and synthesized an answer.
- 2026-04-19 — Phase 3 code-complete (pending end-to-end test): (a) `scripts/session_start_project_state.py` — user-level SessionStart hook implementation, computes slug from `$CLAUDE_PROJECT_DIR`, injects `wiki/projects/<slug>/state.md` as `hookSpecificOutput.additionalContext`; auto-creation gated on project markers (`.git`, `.claude/`, `CLAUDE.md`, `package.json`, `pyproject.toml`, `Cargo.toml`, `go.mod`, `pom.xml`, `build.gradle(.kts)`, `Gemfile`, `composer.json`, `.csproj`, `mix.exs`) per 2026 hooks research — prevents throwaway-folder pollution; (b) `~/.claude/hooks/session_start.sh` wrapper shim (machine-local) — discovery in home, impl in vault; shim safely no-ops if `$LLM_WIKI_ROOT` unset or impl missing; (c) registered in `~/.claude/settings.json::hooks::SessionStart` with matcher `startup|resume|clear|compact` (matches project-level convention); (d) comprehensive offline testing: vault cwd injects state, markerless cwd no-ops cleanly, unset env silent, broken impl path silent, markered test folder auto-creates and injects correctly. Audit passed 6 categories (functionality, security, reliability, integration, docs, legacy). Pending: real-world test in a fresh Claude Code window → commit.
- 2026-04-19 — Phase 3 end-to-end verified (committed as `e8179e7`): opened fresh Claude Code window in a new markered folder (`<test-folder> with `.git/`) — hook fired, `wiki/projects/test-hook-phase3/state.md` auto-created from template with slug and project root filled in, content injected into Claude's session context, Claude correctly reported the fresh state. Zero entries in `hook-errors.log`. Test artifact cleaned up.
- 2026-04-19 — Phase 3.5 complete: (a) `scripts/session_end_project_tag.py` — user-level SessionEnd hook that appends a tagged entry to `memory/daily/YYYY-MM-DD.md` when cwd is outside the vault (vault cwd is handled by existing project-level `session_end_capture.py` via `flush_memory.py`, so the user-level hook skips that case to avoid duplicates); (b) `~/.claude/hooks/session_end.sh` wrapper shim following the same machine-local/vault-impl split as session_start; (c) registered in `~/.claude/settings.json::hooks::SessionEnd`; (d) atomic `.tmp + os.replace` write in `session_start_project_state.py` for first-visit concurrency safety; (e) removed 14 lines of unreachable code in `scripts/lookup_mode.py` (dead fallback after a `return info`); (f) added `wiki/projects/llm-wiki/re-setup.md` — checklist for restoring the global multi-project harness on a new machine (env vars, `~/.claude/CLAUDE.md`, user-level skills, hook wrappers); (g) updated [[Global Multi-Project Migration Plan]] Execution status — Phases 0–3.5 done, Phase 4 (soak test) in progress, Phase 5 (polish) pending.
- 2026-04-19 — Phase 4 soak test (accelerated — 51 automated scenarios + 5 manual end-to-end checks in fresh Claude Code windows) surfaced three bugs, all fixed in a single `fix:` commit: (1) `LLM_WIKI_STATE_ROOT` was missing from user-level `~/.claude/settings.json::env` — hook errors silently vanished. Fix: added the env var explicitly + added a `_resolve_state_root()` fallback (`$LLM_WIKI_ROOT/../LLM-wiki-state`) in both hook scripts so error logging works even if the env var is forgotten. (2) `_compute_slug` defense-in-depth: slugs `.`, `..`, and empty all now map to `"root"`. `Path.resolve()` in `_resolve_project_dir` normally prevents these, but the explicit guard costs nothing and blocks any future refactor that might skip resolve. (3) `_has_project_marker` false-positive on `$HOME`: user's home dir contains `~/.claude/` (user-level Claude Code config) which matched our `.claude` project marker, so casually launching Claude Code from `C:\Users\User` auto-created a nonsense `wiki/projects/user/state.md` and tagged 7 daily-log entries `Project slug: user`. Fix: both hooks now check `if project_dir == Path.home(): skip`. Cleanup: removed `wiki/projects/user/` artifact and the 7 `user`-slug SessionEnd entries from today's daily log. Other soak results — all positive: 3-reopening state.md persistence verified, /compact re-injection verified, cross-project isolation verified, real-project slug (`your-app` from `<your-projects-dir>/your-app`) verified readable, `hook-errors.log` stayed clean post-fixes.
- 2026-04-19 — Phase 5 polish complete. (a) `scripts/vault_editorial.py` — shared module centralizing `EDITORIAL_NAMES`, `BACKLINK_EXEMPT_NAMES`, `BROKEN_LINK_SKIP_NAMES`, and the `editorial_parents_to_skip()` helper. Both `lint_memory.py` and `lookup_mode.py` now import from it, eliminating the prior drift-prone duplicate literals. (b) `scripts/archive_daily.py` — dormant infrastructure for moving compiled `memory/daily/*.md` older than 90 days into `memory/daily/archive/YYYY-MM/` once the flat directory crosses 30 files. Silent no-op below threshold; flags `--commit`, `--force`, `--threshold`, `--max-age`. Never moves un-compiled logs. Not wired into any hook yet. (c) Plugin packaging researched and **deliberately skipped**: Claude Code plugin architecture in April 2026 doesn't support shipping a CLAUDE.md-equivalent, modifying user-level env vars, or hosting hooks that reference scripts outside the plugin cache. Not an architectural fit for our mandatory machine-local harness. Recorded verdict in plan + re-setup guide remains the correct install path. (d) Symlink migration for `~/.claude/skills/knowledge-lookup/` attempted and skipped: Windows refuses symlink creation without Developer Mode/admin elevation, plus user-level and project-level variants of the skill intentionally diverge (absolute vs relative paths). Copy-with-divergence is the right pattern.
- 2026-04-19 — External architect/auditor review surfaced 7 issues in scripts that pre-dated the multi-project work; fixed in three rounds. **Round 1 (`1039aae`)** — critical correctness: (a) `rebuild_memory_index.py` replaced hardcoded `$LLM_WIKI_ROOT` with `memory_state.ROOT` import (portable); (b) `compile_memory.py` now gates `compiled_daily_hashes` writes on actual success (`_compile_succeeded()` detects SDK missing / exception / missing COMPILE_DONE marker), closing a silent-data-loss class bug where failed compiles were marked as done; (c) `rebuild_index()` in both `compile_memory.py` and `query_memory.py` returns `bool`, callers surface warnings, `state.json::last_index_rebuild_ok` tracks outcome. **Round 2 (`ff0fbad`)** — slug collision that was documented but not implemented: `session_start_project_state.py::_compute_slug` now runs full disambiguation (base → parent-of-parent → git `owner-repo` from `.git/config` → grandparent → 6-char path-hash); SessionEnd looks up SessionStart's slug via `- Project root:` match for consistency. Two projects named `backend/` now get distinct stable slugs instead of silently merging. **Round 3 (`ed166d9`)** — quality: (I4) `session_start_context.py` strips `Trigger:`, `Transcript:`, `Project root:`, and session-id UUIDs from injected context, dropping size from 2432 → 2102 chars; (I5) `query_memory.py::slugify` uses `[^\w]+` with `re.UNICODE` + deterministic 6-char hash suffix so Cyrillic questions no longer collapse to `"question"`; (I6) `flush_memory.maybe_trigger_compile` adds `MEMORY_COMPILE_COOLDOWN_SECONDS` (default 900s) preventing compile-per-session-end on busy post-18:00 days.
- 2026-04-19 — **Round 5+6 (post-audit hygiene)**. Round 5 (`bad8b75`) closed the remaining important findings from the second colleague pass: (a) `pyproject.toml` loosened from `>=3.14` to `>=3.10` — the whole codebase runs on 3.13 cleanly, artificial upper pin removed (uv.lock regenerated accordingly); (b) `_slug_owns_dir` made strict — state.md without a `- Project root:` line is NOT claimed, forces disambiguation, worst case is a hash-suffixed slug (safer than cross-contamination); (c) `query_memory.slugify` edge case — punct-only / emoji-only inputs (`???`, `!!!`, `💥`) now get `question-<6hex>` instead of colliding on bare `question`; (d) docs drift: `session_start_project_state.py` module docstring + `wiki/projects/README.md` rewritten to match authoritative 5-step slug rule in `~/.claude/CLAUDE.md`; (e) README stale count 14→16 + softened `$LLM_WIKI_STATE_ROOT/` absolute paths to `$LLM_WIKI_STATE_ROOT/...` portable form. Round 6 added the three items the third review asked for: (f) `docs/EXPORTING.md` — explicit `git archive HEAD` recipe replacing the risky `zip -r` that was leaking `.venv/`, `settings.local.json`, `gitleaks-report.json` into distribution archives; (g) `tests/` — 37-test pytest regression suite covering slug collision, compile fail-safe, context noise stripping, Unicode slugify, SessionEnd skip semantics (all pass in ~2s); (h) `.claude/settings.local.json` narrowed from `Read(//d//**)` (full D: drive) to explicit `D:/LLM-wiki/`, `D:/LLM-wiki-state/`, `D:/dev/` scopes, plus removal of dead references to deleted scratch tests. File stays local-only / gitignored. Colleague rating: **8.3/10 production-readiness**.
- 2026-04-19 — **Round 7+8 (hermetic tests, portability polish, CI)**. Round 7 (`9d28653`) made the regression suite runnable from a fresh clone: `tests/conftest.py` now bootstraps `LLM_WIKI_ROOT` and `LLM_WIKI_STATE_ROOT` via `setdefault` and creates a skeleton `state.json` if absent — previously `pytest` failed on a clean-clone audit because subprocess-invoked hooks silently no-op'd without env. Also removed the last Windows-specific hardcoded path from runtime logic (`lookup_mode.py::qmd_status`) and replaced with env-derived priority list (`$QMD_INDEX` → `$LLM_WIKI_STATE_ROOT/qmd/...` → sibling default). Round 8 closed the three repo-actionable items from the fourth full audit: (a) deleted stale `scripts/bootstrap-qmd.ps1` (hardcoded `$LLM_WIKI_ROOT`, called a sibling `qmd.ps1` that no longer exists) and replaced with portable `scripts/bootstrap_qmd.py` invoking `qmd` from PATH, with idempotent collection-add semantics; (b) `scripts/archive_daily.py` docstring drift — "memory/state/state.json" → canonical "$LLM_WIKI_STATE_ROOT/memory-state/state.json"; (c) `.github/workflows/tests.yml` — first CI workflow, runs structural lint + hermetic pytest on push/PR to main, uses `uv` with Python 3.10 (matches `pyproject.toml` minimum) so future regressions surface automatically instead of socially.
- 2026-04-19 — **Round 10 (audit v6: bootstrap abort path, local lint parity, export helper)**. Closed the three repo-actionable items from the sixth full audit. **H1 bootstrap_qmd main() special-case:** after R8 moved the "already-registered tolerance" logic INTO `add_collections()` (so it only returns non-zero on real failures), the `main()` loop still special-cased `step != "collections"` and ignored real failures on that step. Removed the special case — every step now aborts the full bootstrap on any non-zero, so a path-missing / permission-denied / DB-lock failure on collection registration no longer lets the script continue through `update`/`embed`/`status` and look successful. **M1 pre-commit parity with CI:** `.pre-commit-config.yaml` structural-lint hook was advisory (`uv run python scripts/lint_memory.py`) while CI was blocking (`--fail-on-findings`). Local commits could pass despite structural findings, only CI would catch them. Brought pre-commit to parity — now uses `--fail-on-findings` with the same `orphan_daily_logs` default exemption. **M2–M5 export helper:** colleague audit keeps finding `.venv/`, `.git/`, `settings.local.json`, `gitleaks-report.json`, `workspace.json` in delivered zips. Root cause: operator uses `zip -r` which bypasses `.gitignore`. Added `scripts/export_vault.py` — one-command wrapper around `git archive` with built-in post-export verification: lists archive contents, checks for forbidden path patterns, fails loudly if any slipped in. Supports `--format zip|tar|tar.gz`, `--ref <ref>`, `--strict` (fail on dirty working tree), `--no-verify`. Updated `docs/EXPORTING.md` TL;DR to point at the new script first, with raw `git archive` as the underlying recipe. Verified on a real run: produced 133-file / 0.22 MB zip, verification passed. Still 37/37 pytest + lint clean.
- 2026-04-19 — **Round 9 (audit v5: honest docs + enforced CI + mojibake cleanup)**. Fifth colleague full audit surfaced two High and three Medium findings, all repo-actionable; closed in one commit. **H1 auto-commit drift:** docs (plan, state.md, `~/.claude/CLAUDE.md`) still said "Auto-commit yes, auto-push no" but no hook ever implemented auto-commit. Downgraded all three docs to "No auto-commit, no auto-push — commits are manual and deliberate". **H2 advisory CI lint:** `lint_memory.py` always exited 0, so the CI step was cosmetic. Added `--fail-on-findings` flag + `--allowed-categories` exemption list (default exempts `orphan_daily_logs` which self-resolves on next compile); CI workflow now uses it. Structural findings fail the build. **M1 state.md contradictions:** page had "10 commits local", "6 local commits ready", "currently 16", "currently 15" within the same screen. Rewrote "Where we left off" to reference `git log origin/main..HEAD | wc -l` and `python scripts/lookup_mode.py` for live counts instead of hardcoding numbers that rot. Also synced slug rule from 2-step to actual 5-step. **M2 phantom `--project <slug>` flag:** plan Phase 3 step 4 described a `compile_memory.py --project <slug>` feature that was never built. Relabeled as "dropped as superseded". **M4 `bootstrap_qmd.py` swallowed every non-zero return from `qmd collection add`:** now uses `_run_capture()` + `_ALREADY_EXISTS_MARKERS` heuristic — only known "already registered" outcomes are non-fatal; real failures (path-not-found, permission, DB lock) abort with actionable error. **M5 mojibake in `memory/daily/2026-04-13.md`:** 14 corrupted lines (Russian Cyrillic double-encoded cp1251→utf-8 + em-dashes as `вЂ—`). Fixed with single-pass strict cp1251→utf-8 roundtrip (falls back to original on UnicodeError — preserves legitimate Cyrillic) + manual `\u2017` → `—` replacement. All 14 lines recovered to correct Russian/punctuation. Bumped `compiled_daily_hashes['2026-04-13.md']` in state.json to the post-fix hash (encoding-only change, no durable-content drift → no re-compile needed).
- 2026-06-12 — Added YourApp project operating rules for Codex: root `AGENTS.md` plus frontend/backend-specific `AGENTS.md` files now capture product context, stack-specific implementation rules, SEO/API/lead-flow risks, safety constraints for existing uncommitted work, and verification expectations. Also rewrote the Desktop task prompt `Правила постановки задачи.txt` as a YourApp-specific implementation prompt. Registered project state at [[wiki/projects/your-app/state|YourApp — State]].
## Editorial note
This log is vault metadata — an editorial changelog, not content derived from a raw source. Entries are appended in chronological order; prior entries are preserved rather than rewritten. See [[Review Workflow]] for cadence.

**Historical entries may describe behavior that has since been superseded.** Examples that remain in this log by design: early "auto-commit yes / auto-push no" decisions (superseded — see latest entries, commits are manual), references to the pre-centralization `memory/state/state.json` path (superseded by `$LLM_WIKI_STATE_ROOT/memory-state/state.json`), and an earlier hardcoded `$LLM_WIKI_STATE_ROOT/qmd\index.sqlite` (superseded by env-derived resolution in `scripts/lookup_mode.py::qmd_status`). Each item's newer entry supersedes the older without rewriting history. When the log disagrees with current code, code is the source of truth — see `[[Global Multi-Project Migration Plan|Reliability notes]]` for the precedence rule.




