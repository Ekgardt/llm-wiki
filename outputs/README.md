# outputs/

Exportable artifacts derived from `wiki/` — rendered markdown, Marp slides, matplotlib images, bundles for sharing. Per CLAUDE.md step 4 of the project structure.

Contents are regenerated from `wiki/` sources; nothing here is canonical. Safe to delete and rebuild.

## Editorial note
This file is a placeholder so `outputs/` exists as a real directory referenced from CLAUDE.md. Remove once the first real export artifact is generated.
